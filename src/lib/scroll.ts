/**
 * Offset used for both the CSS scroll-margin on anchored sections and the
 * scroll-spy threshold in Nav. They must match: if the browser lands a
 * scrolled-to section at this many px from the top, the spy has to consider
 * that same distance "reached," or the highlighted nav link sits one section
 * behind wherever the page actually scrolled to.
 */
export const SCROLL_OFFSET = 84;
